{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cp -r ../build/web ./www && npm install",
    "start": "node index.js",
    "deploy": "gcloud run deploy icici-complex-screen  --source . --platform managed --region asia-south1 --allow-unauthenticated"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "helmet": "^6.0.1"
  }
}
